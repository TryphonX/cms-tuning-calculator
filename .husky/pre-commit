#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running pre-commit checks..."

# Run lint-staged (ESLint + TypeScript for staged files only)
echo "� Running lint-staged..."
npx --no-install lint-staged

echo "✅ All pre-commit checks passed!"
